import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://calderonrigoberto.github.io/presentacion-ejecutiva-fundacion',
  base: '/presentacion-ejecutiva-fundacion/',
  compressHTML: true,
});

